import React from "react";
import { Link } from "react-router-dom";

const LogoComponent = () => {
    return (
        <h1>
            <Link to="/">OpenLib</Link>
        </h1>
    );
};

export default LogoComponent;
